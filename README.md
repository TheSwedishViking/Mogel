Essential changes from main;

- Create a dictionary containing a date and a list of value-holder => DataBracket
- Added a foreach loop to loop through all available weather data
- Data bracket contains info about time, temperatur, is inside or outside, and humidity
- Every new date, create a new list, and add a data bracket of info, by minute (per provided weather data)
  
